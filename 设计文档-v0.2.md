# 问卷可视化分析工具 · 设计文档 v0.2

> 版本记录规则：每次设计有实质性变化，另存一份新版本文件（v0.2、v0.3…），不覆盖旧版本。v0.1 见同目录 `设计文档-v0.1.md`。
> 本文件 v0.2，形成于 2026-09-13。相对 v0.1 的变化见文末「版本历史」。

---

## 1. 项目背景

内部已有一套问卷分析 SOP，目前以两份 skill 文件的形式存在（`问卷整理_Word汇报版-1.1` / `问卷整理_Excel原始数据版-1.1`，均在 `~/Desktop/Skills/`），靠人在 Claude Code 对话里手动执行。问题：
- 每次都要重新走一遍确认数据、清洗、画图的流程，重复劳动；
- 图表规则、数字格式、清洗底线这些"硬规矩"写在 prompt/skill 文字里，AI 遵守程度不稳定，容易偷懒漏做；
- 给客户看的是静态 Word/Excel，交互性和可视化表现力有限。

目标：做一个网页工具，把 SOP 里能写成确定性规则的部分**变成 Python 代码**，只在 Python 真正做不了的地方（开放题语义分类、双语翻译）按需调用 AI API。参考 Credamo 的分析能力做视觉/交互上的提升，用途：内部团队分析 + 未来可授权客户只读浏览。

---

## 2. 当前阶段范围

**做：**
- 本地单机运行、CSV 上传分析、图表可视化、原始数据可折叠查看、按需触发 AI 分类/翻译。
- **优先级铁律：两份 SOP skill 里已经写死的规则，是 v1 的验收基线，必须全部实现，不能因为要做"参考 Credamo 的新功能"而绕过或简化。** SOP 覆盖清单见第 6 节。Credamo 式的亮点功能是基线做完之后的拓展 backlog，见第 7 节。

**暂不做（明确推迟）：**
- 部署上线（先本地测试打磨）；
- 内部团队登录/多账号体系；
- 客户只读分享链接的鉴权落地（表结构留位置，逻辑不写）；
- 定性调研、社媒信息调研两个板块（不设计、不在 UI 里预留入口，只在第 9 节定几条不冲突原则）。

---

## 3. 已确认的关键决策（截至 v0.2）

| 决策点 | 结论 |
|---|---|
| 整体技术路线 | **自建，不 fork 现成问卷平台**（XIAOJUSURVEY/SurveyKing/DWSurvey 等评估结论见第 5 节）；可以参考它们和 Credamo 里有价值的功能点，但代码自己写 |
| SOP 与拓展功能的关系 | SOP 已有内容是必须先完成的基线；参考其他工具得到的新点子是后续可拓展 backlog，不能本末倒置 |
| 应用分层 | 分析引擎（纯 Python，无框架依赖）与 UI 外壳分离；外壳先用 Streamlit 起步，以后要换 React 只换外壳 |
| 部署形态 | 先本地测试，部署方案后定；如果部署，目标是客户只读链接，不需要客户账号体系 |
| 内部团队登录 | 后期再加，当前单用户本地使用，不做鉴权 |
| AI 开放题分类 | 两种模式都要支持，每次触发时勾选："封闭分类"（预定义类目）或"开放聚类"（AI 自己总结，可事后固定为封闭类目复用） |
| 文件格式（v1） | CSV 优先，解析层从一开始按 CSV + xlsx 两种写，UI 先只露 CSV 入口 |
| 语言 | 需要双语（英文问卷原文 + 中文报告），开放题要保留「原文｜中文翻译」双列，翻译可用 AI 辅助但要走数字/品牌名校验 |
| Python vs AI 边界 | 确定性计算（计数、占比、排序、图表选型、清洗规则）一律 Python；AI 只做语义判断（开放题分类、逐句翻译），输出必须经 Python 校验，不直接采信 |

---

## 4. 架构

**分层原则：分析引擎和 UI 外壳彻底解耦，UI 框架以后想换随时能换。**

```
问卷可视化分析工具/
├── engine/                 # 纯 Python，不 import 任何 Web/UI 框架
│   ├── ingest.py           # CSV/xlsx 解析、编码探测
│   ├── clean.py            # 去重、screen out、A/B 合并
│   ├── stats.py            # 分布、排名、均值中位数、交叉表
│   ├── chart_spec.py       # 统计结果 → 图表配置（图型/配色按 SOP 规则）
│   ai_classify.py        # 开放题分类（封闭/开放两种模式）+ 校验
│   ai_translate.py       # 逐句翻译 + 数字/品牌名校验
│   └── export_word.py      # 导出 Word 汇报版（复用 SOP 排版规则）
├── app_streamlit/          # UI 外壳，只做展示和交互，业务逻辑都调 engine
│   └── pages/...
├── data/                   # SQLite 文件、上传的原始文件缓存
└── tests/                  # engine 的单测——SOP 里每条硬规则对应一条测试
```

- **后端/应用**：Streamlit（本地 `streamlit run` 即可，无需单独前端构建）。原生 `st.expander` 对应"图表在上、原始数据默认折叠"的布局需求；图表用 `streamlit-echarts` 或 Plotly。
- **数据库**：SQLite，单文件。
- **AI 调用**：Anthropic API，封装在 `engine/ai_classify.py` / `engine/ai_translate.py` 里，UI 层不直接碰 API key。

**预留但不实现**（给部署/登录留口子，现在不花时间做逻辑）：
- `share_links` 表结构定义好，暂无路由使用。
- 用户身份走一个 `get_current_user()` 占位函数，固定返回本地用户。

---

## 5. 开源方案评估结论（为什么不 fork）

评估了 XIAOJUSURVEY（滴滴，Vue3+NestJS+MongoDB）、SurveyKing、DWSurvey（均为 Java/Spring Boot+Vue3）三个候选，以及国际上的 Formbricks（Qualtrics 开源替代，Next.js）。结论：

- 四者都是**问卷收集平台**，数据模型假设"问卷在本系统里建、答案在本系统里收"；你的实际工作流是反过来的——数据在外部平台（Prolific/Qualtrics/问卷星等）收集完，拿 CSV 导入分析。
- 它们的"分析"功能停留在单题统计 + 简单两题交叉这个量级，达不到 SOP 要求的开放题语义分类、双语摘录表、闭集式报告结构。
- 全部是非 Python 技术栈，和"Python 优先、AI 只做兜底"的原则天然不合，fork 后要么在旁边再起一个 Python 服务（双栈集成成本），要么放弃 Python-first。

**结论：不 fork 问卷收集平台，但保留它们的功能设计作为参考**（比如 SurveyKing 的交叉分析交互、Credamo 的报告呈现方式），思路借鉴、代码自己写。Python 生态里真正值得复用的是构建块级别的开源项目（Streamlit、streamlit-echarts、pandas、rapidfuzz），这些已经在架构里用上了。

---

## 6. SOP 覆盖清单（v1 验收基线，来自 skill 1.1）

这张表是 `engine/` 模块的验收标准，每一行都应该对应至少一条单测。

| 分类 | 规则 | 对应模块 |
|---|---|---|
| 清洗 | 去重、剔除 screen out、剔除未进 B 的 A 样本、A/B 同人合并、异常脱落先报告 | `clean.py` |
| 数字格式 | 百分比一律 `XX.X%`；写成"XX人（XX.X%）"，禁"大多数/部分用户" | `stats.py` 输出格式化 |
| 图表选型 | 单选 ≤5 饼图；单选 >5 横向柱；多选横向柱 | `chart_spec.py` |
| 图表文案 | 图内标题=问题概括，禁"人数与占比"废话标题；图下角标=题号+概括+n | `chart_spec.py` |
| 开放题 | 默认只出一张摘录表（对应选择｜原文｜中文翻译）；核心发现一句话带最高频主题人数占比；仅需要完整分布时才加主题概括表 | `stats.py` + `ai_classify.py` |
| 数值题 | 填具体数字→均值-中位数表；选区间→按单选处理，不把中点当 Min/Max | `stats.py` |
| 排序 | 无序项按主指标降序；有序项（价格区间/量表/年龄段）保原序 | `stats.py` |
| 核心结论 | 每条带数字；同一对象多维度时合并到一条比较，不拆条 | `export_word.py` / 报告生成层 |
| AI 洞察 | 可选收尾，没有增量就整节不出 | `export_word.py` |
| 翻译 | 逐句翻译非概括；数字与品牌名原样保留，Python 校验不通过打回 | `ai_translate.py` |
| 命名/互链 | 问卷名+日期`-MMDD`；双版本加 `-Word`/`-Excel` 后缀并互链 | `export_word.py` |
| 底线 | 只做现象判断不做商业判断；闭集精简，没有 skill 要求的元素不出现 | 贯穿全部生成逻辑 |

---

## 7. 拓展功能 backlog（v1 基线做完之后再看）

不阻塞 v1，但记下来免得忘：

- 交叉表的统计显著性检验（卡方检验等，需要 scipy/statsmodels）
- 点击图表下钻到原始回答（Streamlit 里能做但比较绕，React 版更顺手）
- 开放聚类结果的可视化（比如主题词云、相似回答聚类展示）
- 多问卷/多轮测试的历史对比看板（类似地毯项目那种"第一轮/第二轮/分流测试"的纵向对比）
- 报告一键导出为可分享的网页快照，而不只是 Word

---

## 8. 风险与坑

- **列映射靠人工，无法完全自动化**——CSV 不会自描述题型，映射表单这一步不能省。
- **CSV 编码/引号问题**——中文导出常见 GBK/UTF-8-BOM 混用，用 pandas 健壮读取而不是手写 split。
- **开放聚类的类目不稳定**——设计了"固定为封闭类目"的转换路径，否则没法跨批次比较。
- **AI 调用成本/延迟**——按需触发，不自动全跑。
- **品牌名词表需要维护**——首次搭建要录入已知品牌名，后续遇到新品牌记得加。
- **Streamlit 的交互上限**——精细的点击下钻类交互比 React 麻烦，属于已知取舍，写进了 backlog 而不是死磕。

---

## 9. 跨板块边界原则（定性调研 / 社媒调研，现在不做，只保证不冲突）

1. 各板块各自独立项目文件夹、独立数据库，不共用同一个 SQLite 文件。
2. 定性调研工具（interview-crosstab skill）走"生成自包含静态 HTML，无后端"的路数，和本工具"跑后端+AI+数据库"架构不同，不需要统一，保持各自最适合自己场景的形态。
3. "Python 算数字、AI 只做语义判断且留审计记录"（对应 `ai_runs` 表）是通用原则，以后其他板块接 AI 可以照搬思路，不需要现在抽公共库。
4. 本地端口等资源写成可配置项，避免以后几个工具同时在本机跑时抢端口。

---

## 10. 数据模型（SQLite，草案）

```
projects(id, name, source_lang, target_lang, created_at)
documents(id, project_id, filename, format, uploaded_at)
questions(id, document_id, q_no, q_type, source_text_en, order_index, is_screen_out)
responses(id, question_id, respondent_id, raw_value, translation, order_index)
categories(id, question_id, label, source)         -- source: manual / ai_closed / ai_open_frozen
response_categories(response_id, category_id)
ai_runs(id, question_id, mode, prompt_version, raw_output, created_at)   -- 审计用
share_links(id, project_id, token, scope, expires_at)   -- 预留，当前无路由使用
settings(key, value)
```

---

## 11. 待确认问题（开始写代码前）

1. AI 供应商/模型：直接用 Claude API，还是要支持切换供应商？
2. 开放题预定义类目：由你在映射页手动输入，还是要一版"常见类目"起点模板（品质/价格/设计/其他）供你改？
3. 品牌名词表、专业词表（cut pile / loop pile 这些）复用现有 skill 参数表，还是另建更完整的一份？
4. 第 7 节的 backlog 里，有没有哪几项其实想提到 v1 里，不想等？

---

## 12. 里程碑（本地 MVP，Streamlit 路线，不含部署阶段）

1. 第 1 周：`engine/` 骨架 —— CSV 解析、数据映射、清洗层、SQLite；对应 SOP 清单里的清洗规则先写测试
2. 第 2 周：统计引擎 + 图表配置层，Streamlit 页面把图表和折叠原始数据区跑起来
3. 第 3 周：AI 分类（封闭+开放两种模式）+ AI 翻译校验
4. 第 4 周：导出 Word、内部试用、对照第 6 节清单逐条验收

---

## 版本历史

- **v0.1**（2026-09-13）：首次成文，FastAPI+React+SQLite 方案。
- **v0.2**（2026-09-13）：确认不 fork 现成问卷平台（评估了 XIAOJUSURVEY/SurveyKing/DWSurvey/Formbricks，结论见第 5 节）；架构改为「纯 Python 分析引擎 + Streamlit 外壳」分层，为以后换 React 留口子；新增第 6 节 SOP 覆盖清单作为 v1 验收基线，新增第 7 节拓展 backlog，明确"SOP 基线优先于新功能"的优先级；新增第 9 节跨板块边界原则。对应 Claude Code 会话见同目录 `resume-claude-code.md`。
