# 本地虚拟环境——同事自己按 README 装，不用传
.venv/

# 本地数据——里面是真实客户问卷数据、AI 分析结果，以及在设置页面填的 API key
# （明文存在 app.db 里，没有加密）。这些东西只应该留在自己电脑上，
# 绝对不能传到 GitHub 上（哪怕是私有仓库也不建议，私有仓库的协作者一样能看到）。
data/app.db
data/app.db-journal
data/app.db-wal
data/app.db-shm
data/backups/
data/uploads/
data/exports/
data/translation_cache.json

# Python 缓存/测试产物
__pycache__/
*.pyc
.pytest_cache/

# 系统/编辑器杂项文件
.DS_Store

# 临时验证脚本，不属于正式代码
verify_streamlit_render.py
resume-claude-code.md
路线图.md

# 内部团队版专用的共享 API key（真实凭据，绝对不能进公开仓库的 git 历史）——这个
# 文件只应该存在于私有仓库 QueryStats-Tool-Internal 的 internal 分支上，那边的
# .gitignore 不会排除它（各分支的 .gitignore 内容本身也是可以不一样的）。这里排除
# 是防止本地切分支时残留的文件被 `git add -A` 误提交进这个公开仓库，不是"这个文件
# 不重要"的意思——见 engine/internal_defaults.py 文件头的完整说明。
engine/internal_defaults.py
