# 问卷可视化分析工具 · 设计文档 v0.3

> 版本记录规则：每次设计有实质性变化，另存一份新版本文件，不覆盖旧版本。v0.1/v0.2 见同目录。
> 本文件 v0.3，形成于 2026-09-13。相对 v0.2 的变化见文末「版本历史」。**Milestone 1（engine 核心：ingest/clean/stats/chart_spec/db）已实现并独立验证通过，代码在 `engine/`、测试在 `tests/`。**

---

## 1. 项目背景

（同 v0.2，未变）内部已有一套问卷分析 SOP（`~/Desktop/Skills/` 下两份 skill 1.1 文件），靠人在 Claude Code 对话里手动执行，重复劳动、规则遵守不稳定、给客户看的是静态文件交互性有限。目标：把 SOP 里能写成确定性规则的部分变成 Python 代码，AI 只在真正做不了的地方（开放题分类、双语翻译）按需介入，参考 Credamo 的分析能力做视觉/交互提升。

---

## 2. 当前阶段范围

**做：** 本地单机运行、CSV/xlsx 上传分析、图表可视化、原始数据可折叠查看、按需触发 AI 分类/翻译，AI 供应商可插拔切换。

**优先级铁律：** 两份 SOP skill 里已经写死的规则是 v1 验收基线（见第 6 节），必须全部实现；Credamo 式的亮点功能是基线做完之后的拓展 backlog（见第 7 节），v0.3 已确认 **backlog 全部不提前**，先把基线走完。

**暂不做：** 部署上线、内部团队登录、客户只读分享链接鉴权落地、定性调研/社媒调研板块（只定边界原则，见第 9 节）。

---

## 3. 已确认的关键决策（截至 v0.3）

| 决策点 | 结论 |
|---|---|
| 整体技术路线 | 自建，不 fork 现成问卷平台（评估结论见第 5 节） |
| SOP 与拓展功能的关系 | SOP 基线优先于新功能；backlog 全部不提前到 v1 |
| 应用分层 | 分析引擎（纯 Python，无框架依赖）与 UI 外壳分离；外壳用 Streamlit |
| 部署形态 | 先本地测试，部署方案后定 |
| 内部团队登录 | 后期再加 |
| AI 开放题分类 | 支持"封闭分类"（预定义类目）和"开放聚类"（AI 提炼类目，可固定复用）两种模式，运行时勾选 |
| **AI 开放题类目来源**（v0.3 新增） | **不建跨项目通用模板库**。开放聚类模式下，AI 直接从当前这道题的实际回答内容里提炼候选类目——类目跟着具体数据走，不是套一份"品质/价格/设计/其他"这种预设标签；封闭模式的类目来源有两条路：人工手动输入，或者把某次开放聚类的结果人工审核后固定下来复用。两条路径都保留在 UI 里，不互斥。 |
| **AI 供应商**（v0.3 新增） | **从一开始做成可插拔**，不锁死 Claude。新增 `engine/llm_provider.py` 定义统一接口，具体供应商（Anthropic 等）各自实现，`ai_classify.py`/`ai_translate.py` 只依赖这个接口，不直接 import 某个厂商 SDK；当前激活哪个供应商存在 `settings` 表里，一个配置项切换。 |
| 品牌名词表/专业词表 | 复用现有两份 skill 参数表起步（cut pile/loop pile 等专业词、Ruggable/IKEA 等品牌名示例），后续项目遇到新词再加，不在这个阶段另建大而全的词库 |
| 文件格式（v1） | CSV 优先，解析层已支持 CSV + xlsx |
| 语言 | 双语（英文问卷原文 + 中文报告），开放题保留「原文｜中文翻译」双列，翻译走 AI 辅助 + Python 数字/品牌名校验 |
| Python vs AI 边界 | 确定性计算一律 Python；AI 只做语义判断（分类、翻译），输出经 Python 校验，不直接采信 |

---

## 4. 架构

```
问卷可视化分析工具/
├── engine/                 # 纯 Python，不 import 任何 Web/UI 框架 —— Milestone 1 已完成 ingest/clean/stats/chart_spec/db
│   ├── ingest.py           # ✅ CSV/xlsx 解析、编码探测
│   ├── clean.py            # ✅ 去重、screen out、A/B 合并、多选合并
│   ├── stats.py            # ✅ 分布、排名、均值中位数、交叉表、数字格式化
│   ├── chart_spec.py       # ✅ 图表选型、ECharts 配置生成
│   ├── db.py                # ✅ SQLite schema + 基础读写
│   ├── llm_provider.py     # 待做（Milestone 2/3）—— AI 供应商可插拔接口
│   ├── ai_classify.py      # 待做 —— 开放题分类（封闭/开放两种模式），只依赖 llm_provider
│   ├── ai_translate.py     # 待做 —— 逐句翻译 + 数字/品牌名校验，只依赖 llm_provider
│   └── export_word.py      # 待做 —— 导出 Word 汇报版
├── app_streamlit/          # 待做 —— UI 外壳，只做展示和交互，业务逻辑都调 engine
├── data/                   # SQLite 文件、上传的原始文件缓存
├── tests/                  # ✅ engine 单测，25 项全部通过
├── .venv/                  # Python 3.11 虚拟环境（已装 pandas/streamlit/anthropic 等）
└── requirements.txt
```

**`llm_provider.py` 接口草案**（供 Milestone 2 实现时参考）：
```python
class LLMProvider(Protocol):
    def complete_json(self, system: str, user: str, schema_hint: str) -> dict: ...
```
`ai_classify.py`/`ai_translate.py` 拿到的是一个 `LLMProvider` 实例（由调用方按 `settings` 表里的配置构造），不知道也不关心背后是 Anthropic 还是别的供应商。当前只需要实现 `AnthropicProvider`，接口先留好，不用现在就真做第二家。

**预留但不实现**（部署/登录相关）：`share_links` 表结构已在 schema 里；用户身份走占位函数。

---

## 5. 开源方案评估结论（为什么不 fork）

（同 v0.2，未变）评估了 XIAOJUSURVEY、SurveyKing、DWSurvey、Formbricks，结论：均为"收集优先"的问卷平台，分析深度停留在单题统计+简单交叉，且都不是 Python 栈。不 fork，思路借鉴、代码自建。

---

## 6. SOP 覆盖清单（v1 验收基线）

Milestone 1 已覆盖以下几行（✅），其余在后续阶段实现：

| 分类 | 规则 | 对应模块 | 状态 |
|---|---|---|---|
| 清洗 | 去重、剔除 screen out、剔除未进 B 的 A 样本、A/B 合并、异常脱落先报告 | `clean.py` | ✅ |
| 数字格式 | 百分比 `XX.X%`；"XX人（XX.X%）" | `stats.py` | ✅ |
| 图表选型 | 单选 ≤5 饼图；>5 横向柱；多选横向柱 | `chart_spec.py` | ✅ |
| 图表文案 | 图内标题=问题概括；图下角标=题号+概括+n | `chart_spec.py` | ✅（配置层已支持，实际渲染在 Streamlit 阶段接入） |
| 开放题 | 默认一张摘录表；核心发现一句话带最高频主题；仅需要时加分布表 | `ai_classify.py` + UI | 待做 |
| 数值题 | 均值-中位数表；区间当单选处理 | `stats.py` | ✅ |
| 排序 | 无序项降序；有序项保原序 | `stats.py` | ✅ |
| 核心结论 | 每条带数字；多维度合并到一条 | `export_word.py` / 报告层 | 待做 |
| AI 洞察 | 可选收尾，没有增量就不出 | `export_word.py` | 待做 |
| 翻译 | 逐句非概括；数字/品牌名 Python 校验 | `ai_translate.py` | 待做 |
| 命名/互链 | 问卷名+日期；双版本互链 | `export_word.py` | 待做 |
| 底线 | 只做现象判断；闭集精简 | 贯穿全部 | 持续把关 |

---

## 7. 拓展功能 backlog（v0.3 确认：全部不提前，v1 基线走完再看）

- 交叉表统计显著性检验（卡方检验）
- 点击图表下钻到原始回答
- 开放聚类结果的可视化（词云/相似回答聚类）
- 多问卷/多轮测试历史对比看板
- 报告一键导出为可分享网页快照

---

## 8. 风险与坑

（同 v0.2，未变，新增一条）

- 列映射靠人工，不能省。
- CSV 编码/引号问题——已在 `ingest.py` 里用 `charset-normalizer` + `utf-8-sig`/`gbk` 兜底处理。
- 开放聚类类目不稳定——靠"固定为封闭类目"路径解决。
- AI 调用成本/延迟——按需触发。
- 品牌名词表需要维护。
- Streamlit 的交互上限——已知取舍，写进 backlog。
- **新增：AI 供应商可插拔意味着 `ai_classify.py`/`ai_translate.py` 的 prompt 设计要考虑跨供应商的输出格式差异**（不同供应商的结构化输出/JSON mode 实现方式不同），`llm_provider.py` 的 `complete_json` 需要在接口这一层就把"返回一个校验过的 dict"这件事做扎实，不能假设所有供应商都原生支持同一种 JSON schema 强制方式。

---

## 9. 跨板块边界原则（定性调研 / 社媒调研，现在不做，只保证不冲突）

（同 v0.2，未变）

---

## 10. 数据模型（SQLite，已实现）

Schema 已在 `engine/db.py` 里实现并通过测试验证，与设计一致：

```
projects(id, name, source_lang, target_lang, created_at)
documents(id, project_id, filename, format, uploaded_at)
questions(id, document_id, q_no, q_type, source_text_en, order_index, is_screen_out)
responses(id, question_id, respondent_id, raw_value, translation, order_index)
categories(id, question_id, label, source)         -- source: manual / ai_closed / ai_open_frozen
response_categories(response_id, category_id)
ai_runs(id, question_id, mode, prompt_version, raw_output, created_at)
share_links(id, project_id, token, scope, expires_at)   -- 预留
settings(key, value)                                     -- 会存 llm_provider 等配置项
```

---

## 11. 待确认问题

（v0.2 列的 4 个问题已在 v0.3 第 3 节决策表里解决，不再重复列出。目前没有新的阻塞性问题——如果 Milestone 2 实现过程中冒出新的，随时补进来。）

---

## 12. 里程碑

1. ~~第 1 周：`engine/` 骨架~~ ✅ 已完成（ingest/clean/stats/chart_spec/db + 25 项测试）
2. 第 2 周：`llm_provider.py`（可插拔接口 + Anthropic 实现）+ `ai_classify.py`（封闭/开放两种模式，类目从实际回答提炼）+ `ai_translate.py`（含数字/品牌名校验）
3. 第 3 周：Streamlit 页面——上传、数据映射表单、图表 + 折叠原始数据区，把 Milestone 1/2 的引擎函数接起来
4. 第 4 周：导出 Word、内部试用、对照第 6 节清单逐条验收

---

## 版本历史

- **v0.1**（2026-09-13）：首次成文，FastAPI+React+SQLite 方案。
- **v0.2**（2026-09-13）：确认不 fork 现成问卷平台；架构改为「纯 Python 分析引擎 + Streamlit 外壳」分层；新增 SOP 覆盖清单（第6节）和拓展 backlog（第7节）；新增跨板块边界原则（第9节）。
- **v0.3**（2026-09-13）：**Milestone 1（engine 核心）已实现并独立验证通过**（委派 Codex 实现，Claude 独立重跑测试+抽查代码逻辑确认无误）。解决了 v0.2 第 11 节的 4 个待确认问题：AI 供应商做成可插拔（不锁死 Claude）；开放题类目不建通用模板，改为 AI 从实际回答提炼 + 保留手动输入；专业词表/品牌名词表复用现有 skill 参数表起步；backlog 全部不提前到 v1。新增 `llm_provider.py` 模块到架构中。对应 Claude Code 会话见同目录 `resume-claude-code.md`。
