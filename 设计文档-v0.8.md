# 问卷可视化分析工具 · 设计文档 v0.8

> 版本记录规则：每次设计有实质性变化，另存一份新版本文件，不覆盖旧版本。v0.1～v0.7 见同目录。
> 本文件 v0.8，形成于 2026-09-14。**新阶段：从"单页 demo、刷新即丢"升级成"多项目、真持久化"的正式应用形态**——
> 首页 + 项目 + 历史分析记录 + 侧边栏模块导航 + 多家 LLM 供应商可选 + Word 导出。
> 新增内容见第 7 节。Milestone 1～6（engine 核心/AI 层/①～⑩ 页面逻辑）作为可复用的底层，本版不推倒重来，
> 只是把它们从"跑在 session_state 里"改成"读写 SQLite"，UI 外壳从"单页"改成"多页"。

---

## 1. 术语（这版新增，后续统一使用）

- **分流**：受访者进入后被随机分配到几份不同的问卷（跨问卷）。对应 `documents` 表里不同的 `branch_label`。
- **跳转**：同一份问卷内部，选了某个选项后跳到指定题目（问卷内）。v1 暂不建模具体跳转规则引擎，先作为「测试方法」里的一段说明文字记录，不影响统计（统计仍按实际填答数据算，缺失值就是缺失值）。

这两个词已经反向同步进了 `~/Desktop/Skills/` 的两份 SOP skill（Word 1.3、Excel 1.2），第 1 步"确认数据"从含糊的"跳题逻辑"拆成了这两条独立确认项。

---

## 2. 整体页面框架（10 部分，用户原始定义）

1. **结论**——一句话核心结论，默认 1 条，可编辑/新增。
2. **测试方法**——(a) 测试平台与样本来源 (b) 分流设计（几份） (c) 样本量（分流则逐份列） (d) 筛选逻辑 (e) 跳转逻辑。
3. **筛选问题**——仅展示 screen out 题目，被筛掉的人不进任何统计。
4. **正式问卷**——与目前 demo 一致。
5. **基础信息探测**——问卷主动收集但非核心、非筛选的背景题（年龄/性别/收入/住址/邮编/家庭结构/房屋类型等）。
6. **原始数据表格**——内嵌网页、支持全屏；平台方收录的个人信息默认隐藏；主动展示区分筛选/正式问卷/基础探测三段，各用一种主题色，顺序固定。
7. **受访者个人视角**——点击原始数据表某行，下方展开该受访者完整作答（顶部一行平台基础信息，下方按实际问卷顺序：筛选→基础信息→正式问卷）。
8. **交叉分析**——手动配置，不预设；维度可增删，先做双题交叉（支持把某题的若干选项圈成一组再对比）。
9. **AI 洞察**——不自动展示，手动点按钮才生成。
10. **受访者信息**——问卷平台自动收录的数据，如实展示。

---

## 3. Schema 变更

对已实现的 `engine/db.py`（Milestone 1）做以下调整：

```
projects(id, name, source_lang, target_lang, created_at)                       -- 不变

test_method(                                                                    -- 新增，一个 project 一行
    project_id INTEGER PRIMARY KEY REFERENCES projects(id),
    platform_source TEXT,        -- 测试方法(a)：如 "Prolific + Tally"
    is_branched INTEGER NOT NULL DEFAULT 0,   -- 测试方法(b)
    branch_count INTEGER,                      -- 测试方法(b)，不分流则 NULL
    screen_out_rule TEXT,         -- 测试方法(d)，无则填 "无"
    skip_logic_note TEXT          -- 测试方法(e)
)
-- 测试方法(c) 样本量不单独存字段，从 documents/responses 实际数据统计出来，
-- 分流时按 documents.branch_label 分组统计，不需要人工重复维护一份数字。

documents(id, project_id, filename, format, uploaded_at, branch_label)         -- + branch_label：
    -- 分流场景下这份文件属于哪一份问卷（如"组1"）；不分流则为 NULL。

questions(id, document_id, q_no, q_type, source_text_en, order_index, section)  -- is_screen_out 换成 section：
    -- section TEXT NOT NULL DEFAULT 'official'
    --   CHECK (section IN ('screen_out','official','background','platform_auto'))
    -- 四态覆盖 10 部分框架里的第 3/4/5/10 部分，一个字段管住"这题属于哪一段"。

responses / categories / response_categories / ai_runs / share_links / settings  -- 不变
```

`is_screen_out`（布尔）→`section`（四态枚举）是唯一的破坏性改动，因为原来只能表达"是不是筛选题"，现在要再分出"基础信息"和"平台自动"两种。这是个纯 Python + SQL 的机械改动（改字段类型、改一处清洗逻辑的判断条件、加数据迁移测试），适合像 Milestone 1/2 一样整体交给 Codex 做，写清楚旧字段怎么映射到新字段（`is_screen_out=1 → section='screen_out'`，其余 `→ 'official'`）即可，不需要我在过程里做架构判断。

---

## 4. 逐部分实现方案

**① 结论**：新增 `conclusions(id, project_id, text, order_index)` 表，默认插入 1 条占位。UI 是一个可编辑列表（增/删/改文本），不需要 AI，纯 CRUD。

**② 测试方法**：一个手动填写的表单（`test_method` 表对应字段），样本量部分自动统计、只读展示，不需要手填。

**③/④/⑤ 筛选问题/正式问卷/基础信息探测**：三者渲染逻辑相同（复用已验证的 `stats`/`chart_spec`），差异是 `questions.section` 过滤条件 + 页面分段标题 + 题号前缀（编号方案见第 5 节，已确认）。**筛选题的统计口径**：`clean.apply_screen_out` 已经把被筛掉的人从"有效样本"里剔除，③里展示筛选题本身的分布时，分母是筛选前的全量（screen out 题目本来就是拿来筛人的，不可能只统计"通过筛选的人"里的筛选题分布，那样会永远 100%）——这条写进 SOP 的"确认数据"清单里，作为一条固定规则，不用每次都问。

**⑥ 原始数据表格（可行性评估）**：

| 需求 | 用 `st.dataframe` 原生能力 | 用 `streamlit-aggrid`（新依赖） |
|---|---|---|
| 内嵌网页 | ✅ 原生支持 | ✅ 原生支持 |
| 支持全屏 | ✅ 近几个版本自带全屏展开按钮 | ✅ 自带 |
| 平台信息默认隐藏、可切换显示 | ✅ 简单——渲染前按 checkbox 过滤要显示的列即可 | ✅ 同样简单 |
| 三段用三种主题色区分表头 | ⚠️ `st.dataframe` 吃 `pandas.Styler`，但历史上对**表头**背景色的支持不稳定，只稳定支持**单元格**着色——用单元格颜色模拟表头分组是可以做但比较别扭 | ✅ 原生支持"列分组 + 分组表头背景色"，这正是 ag-Grid 的强项 |
| 点击一行展开详情（对应⑦） | ✅ 近几版本 `on_select="rerun"` 原生支持行选中回调 | ✅ 同样支持，且和分组表头是同一个组件里一起解决 |

**结论：这条建议上 `streamlit-aggrid`。** 单纯为了⑥可以尝试原生方案凑合，但⑥+⑦一起看，三色分组表头 + 点击选中两个需求都是 ag-Grid 的原生能力、`st.dataframe` 只能勉强模拟一个，一次性引入这一个新依赖能把⑥⑦都解决，比在原生组件上反复打补丁更省事。**难度：中等**——不是啃不动的硬骨头，但也不是"调个参数就完事"，需要专门测一下这个版本的 Streamlit/aggrid 兼容性（这类社区组件版本兼容问题比官方组件常见），建议我先做一个十几行的最小原型验证可行，再正式排进里程碑。

**⑦ 受访者个人视角**：拿到 aggrid 的选中行 id 后，查这个人在 `responses` 表里所有作答，按 `section`（筛选→基础信息→正式问卷，注意顺序和③④⑤在页面上的呈现顺序不同——⑦是"筛选→基础信息→正式"，前面整体页面顺序是"筛选→正式→基础信息"，这个顺序差异是你原话里就有的，不是我搞混，照办）分组渲染，顶部一行展示平台信息（`section='platform_auto'` 的字段横排）。

**⑧ 交叉分析**：UI 包一层在已经验证过的 `stats.crosstab_counts` 外面：选"从哪道题的哪几个选项"当分组（前端多选框选子集，之后转成一个布尔分组列喂给 `crosstab_counts`），选"对比到哪道题"，渲染表+图。v1 范围就是双题，"维度无限增加"按你说的是长期方向，不卡 v1。

**⑨ AI 洞察**：新增 `engine/ai_insight.py`——和 `ai_classify`/`ai_translate` 一个纪律：**Python 先把所有相关统计算好（各题分布、交叉表），把这些算好的数字连同"只做现象判断"的硬性要求一起喂给模型，模型只负责挑出值得说的现象、组织成一两句话，不允许自己编数字**；生成后 Python 校验一遍——洞察文本里出现的每个数字，必须能在喂给它的统计数据里找到匹配项，找不到就整条丢弃不展示，不让模型的幻觉数字上页面。全局一个按钮触发（不是逐题触发），对应你"不主动展示、点了才生成"的要求。

**⑩ 受访者信息**：`section='platform_auto'` 的字段，如实展示，不聚合、不加工——这条现有 Excel skill 的底线（"不改内容"）本来就覆盖了，不用新写规则。

### v0.7 实现状态 + 已知缺口

①～⑩ 全部在 `app_streamlit/app.py`（session_state 版）跑通一遍，`engine/ai_insight.py` 新增（数字校验逻辑和 ai_classify/ai_translate 一个纪律：Python 先把统计算好、序列化成 JSON 喂给模型，模型只挑现象、不许算数，返回后逐条洞察抽取数字、和喂给它的数据做精确 token 匹配——不是子串匹配，"5"不会因为数据里有"45"就被判定为可验证——核对不上的整条丢弃）。55 项 engine 测试全过。

**没做完/明确知道的缺口，不是忘了，是记录下来留着**：

- **⑥⑦用了新依赖 `streamlit-aggrid`**：数据组装逻辑（列分组、多选合并、受访者ID匹配）单独写脚本测过、没有异常；但**分组表头的三种主题色到底渲染得对不对、点一行选中之后 `selected_rows` 到底能不能正确取到数据**——这两件事我没有浏览器，点不了，需要你自己上去点一下。这是这次唯一"没有独立验证就先交出来"的部分，明确标出来，不是含糊过去。
- **⑧ 交叉分析**只支持"对比到哪道题"是单选题；多选题作为对比对象需要另外处理 list 类型答案，这版跳过了。
- **⑨ AI 洞察**目前统计范围是④正式问卷+⑤基础信息，不含③筛选题（screen 数据对"现象洞察"价值有限，先不放进去，要放也简单）。
- **①②的 `conclusions`/`test_method` 还没接上 Milestone 3a 已经建好的 SQLite 表**——现在纯 `session_state`，刷新即丢，跟其它映射状态保持一致，但这意味着"保存项目"这个能力实际上还不存在。
- **`ai_runs` 表的 `question_id` 是 NOT NULL 外键**，⑨ AI 洞察是全项目级别的调用，不属于任何一道题，现在的 `generate_insights` 干脆不记审计（`conn`/`question_id` 传 `None` 跳过）。等真的接 SQLite 持久化时，这里要么给 `ai_runs` 加一个可为空的 `project_id` 独立记录路径，要么专门造一道"虚拟题"来挂——这个需要设计决定，不是现在顺手改的事。

---

## 5. 题号方案（已确认）

三段各用一套独立编号，互不干扰，一眼能看出题目属于哪一段：

- **筛选问题**：`S1`、`S2`、`S3`……（screen 的 S）
- **基础信息探测**：`C1`、`C2`、`C3`……（collecting 的 C，用户特意选 C 不用 B，避免和别的缩写混淆）
- **正式问卷**：保留原始题号（`Q1`、`Q7`……不变）

这条也反过来推翻了 v1.3 Word skill 里"筛选类……保留原题号"那句旧措辞——旧措辞是在没有"基础信息"这个独立分类之前写的，现在筛选题和基础信息题都有了自己的编号体系，不再套用"保留原题号"这条，只有正式问卷题目继续保留原题号。

**更正（v0.6）**：上面这段"之后会一并更新 skill"是 v0.5 写的时候想当然了。S1/C1/Q1 这套编号是**这个网页分析工具专属的页面结构**，不是 Word 汇报文档的结构——Word skill 的"顺序"规则本来就是另一套（核心结论→全样本基础题→分组逐题→交叉对比→AI洞察），Word 文档里也从来没有一个单独标题叫"筛选问题"的板块（被筛掉的人直接从统计里剔除，不专门展示）。这次没有改 Word/Excel 两份 skill，两边刻意保持各自的结构，不做同步。

---

## 6. 里程碑更新

1. ~~engine 核心~~ ✅ Milestone 1
2. ~~AI 供应商/分类/翻译~~ ✅ Milestone 2
3. **Milestone 3** ✅：
   - 3a schema 迁移（`section` 字段 + `test_method`/`conclusions` 表）——Codex 实现 + 独立验证通过。
   - 3b ①②③④⑤ 页面渲染——`st.session_state` 版跑通，①结论增删改、②测试方法(a)(b)(d)(e)手填+(c)自动统计、③④⑤按筛选/正式/基础信息三段独立编号 S/Q/C，③看全量、④⑤看有效样本。
   - 顺带修了一个真实 bug：`if not st.button(...): st.stop()` 会导致点击"生成分析"后，只要再碰下面任何交互控件就把结果区清空——已改成用 `st.session_state` 记"已生成"，不依赖按钮瞬时返回值。
4. **Milestone 4** ✅（有一处未独立验证，见上面"已知缺口"）：⑥ 原始数据表格（`streamlit-aggrid`，三色分组表头，⑩平台信息默认隐藏可切换）+ ⑦ 点击下钻（用受访者ID字符串匹配，不依赖 ag-Grid 返回的行索引）。
5. **Milestone 5** ✅：⑧ 交叉分析——手动选"从哪题圈人群"+勾选取值+"对比到哪题"，复用已验证的 `stats.crosstab_counts`；仅支持对比单选题。
6. **Milestone 6** ✅（部分）：⑨ AI 洞察模块（`engine/ai_insight.py`，数字校验，55 项测试含新增 6 项）。**导出 Word 这部分还没做**，留在下一批。

---

## 7. 新阶段：首页 + 项目 + 持久化（v0.8）

用户原话 6 点要求，逐条落地：

1. 首页，列出不同项目（对应不同客户）。
2. 自动保存机制——问卷分析做完就存住，用一段时间后能看到同时在跑的多个项目。
3. 打开一个项目，能看到这个项目下各种调研信息：定量问卷调研、定性访谈、案头研究……
4. 定量问卷调研里，是这个项目历史上分析过的各份问卷结果列表。
5. 每份问卷结果按①～⑩ 十个模块，在左侧 sidebar 罗列；sidebar 能隐藏；点标题跳转到对应区域。
6. 首页加手动填 API key 的地方，可以选模型供应商：Claude、OpenAI、DeepSeek、Kimi、Qwen、Grok。
7. 加 Word 导出。

### 7.1 信息架构

```
首页（项目列表）
 └─ 新建项目 / 打开项目
      └─ 项目工作区（按调研类型分区，对应第 3 点）
           ├─ 定量问卷调研 —— 历史分析列表（对应第 4 点）
           │    ├─ + 新建分析（走现有①～⑩流程）
           │    └─ 打开某份历史分析 → 分析详情页（左侧 sidebar 导航①～⑩，对应第 5 点）
           ├─ 定性访谈 —— 占位，不做（另一个 skill：interview-crosstab，各走各的，不在这个工具里合并）
           └─ 案头研究 —— 占位，不做
```

**定性访谈/案头研究为什么只占位不做**：定性访谈已经有独立的 `interview-crosstab` skill（自包含静态 HTML，无后端），架构和这个工具完全不同，v0.2 就定过原则"各板块各自独立，不用勉强凑成一个系统"。这次只是在项目工作区里给它们留一个分类入口（点进去写"还没做"），不是要把那个 skill 的功能搬进来。

### 7.2 Schema 变更（在 Milestone 1/3a 基础上再扩一轮）

```
projects(id, name, source_lang, target_lang, created_at, updated_at)   -- + updated_at，首页按"最近活跃"排序用

documents(id, project_id, filename, format, branch_label, uploaded_at,
          research_type, title, updated_at)
    -- + research_type TEXT NOT NULL DEFAULT 'quant_survey'
    --     CHECK (research_type IN ('quant_survey','qualitative','desk_research'))
    --   这一份属于项目里哪个调研分类（对应第 3/4 点）
    -- + title TEXT：这份分析的展示名（默认用文件名，用户可改，历史列表显示这个不是原始文件名）
    -- + updated_at TEXT：每次重新生成分析或编辑结论/测试方法都要更新

questions(..., meta_json TEXT)
    -- + meta_json：一个 JSON 字符串，装这道题除了 q_no/q_type/section/source_text_en 之外
    --   所有还要存住的东西——screen_out 的"未通过取值"列表、AI 翻译出来的中文标题+选项映射、
    --   多选题的分组键和选项显示标签。不新开好几张小表，一个 JSON 字段够用，
    --   以后哪个字段查询频繁了再考虑拆出来。

settings(key, value)   -- 结构不变，新增两类 key：
    -- "llm_provider" / "llm_model"（已有，现在从"环境变量兜底"改成"这张表优先"）
    -- "api_key::<provider>"：手动填的 API key，按供应商分开存（第 6 点）
```

`documents.research_type` 用 CHECK 约束封住三个值，跟 `questions.section` 那次做法一致；`qualitative`/`desk_research` 现在不会真的插入数据（因为对应模块只是占位），约束先留着，等哪天真做了不用再迁移一次。

**持久化的实际读写路径**（这是这次最大的一块工程，不是加个字段就完事）：
- "生成分析"点下去之后，除了在 session_state 里跑现有逻辑，**额外**把这次分析写进数据库：`create_project`（新项目）或复用现有 `project_id`；`add_document`（`research_type='quant_survey'`）；`add_questions`（带 `meta_json`）；`add_responses`（把 `df_all` 逐题逐行写进 `responses`）；`set_test_method`；`add_conclusion`。
- "打开一份历史分析"要能反着来：从 `responses` 表按 `question_id` 分组、`pivot` 回一个类似 `df_valid` 的宽表，喂回给现有的 `stats`/`chart_spec` 函数——**这些函数从 Milestone 1 起就只认 pandas Series/DataFrame，不关心数据从哪来**，这个设计现在派上用场了，不用为了"从数据库读"重写一遍统计逻辑。
- 这一读一写的具体函数（`save_analysis(conn, project_id, df_all, mapping, screen_config, test_method, conclusions) -> document_id` 和 `load_analysis(conn, document_id) -> (df_all, mapping, screen_config, test_method, conclusions)`）机械、边界清楚、可以整体写测试——适合交给 Codex，不需要我在过程里做架构判断。

### 7.3 多供应商 LLM（第 6 点）

Claude/OpenAI/DeepSeek/Kimi/Qwen/Grok 除 Claude 外全部是 **OpenAI 兼容接口**（同一套 `chat.completions` 请求/响应格式，只是 `base_url` 不同）——不需要写 5 个 Provider 类，写一个 `OpenAICompatibleProvider(api_key, model, base_url)`，5 家供应商各自传不同的 `base_url` 复用同一个类即可：

| 供应商 | `base_url` | API key 环境变量兜底 |
|---|---|---|
| OpenAI | `https://api.openai.com/v1` | `OPENAI_API_KEY` |
| DeepSeek | `https://api.deepseek.com/v1` | `DEEPSEEK_API_KEY` |
| Kimi（Moonshot） | `https://api.moonshot.cn/v1` | `MOONSHOT_API_KEY` |
| Qwen（DashScope 兼容模式） | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `DASHSCOPE_API_KEY` |
| Grok（xAI） | `https://api.x.ai/v1` | `XAI_API_KEY` |

默认模型名先填一个当前看起来对的值，**这些供应商的模型命名会变，用户在设置页可以随时改，不是写死不能动的**。JSON 解析/重试一次的逻辑（`_parse_json_object`）已经是供应商无关的，`OpenAICompatibleProvider` 直接复用，不重写。

**安全提示（写进 UI 里，不是只在文档里提一句）**：手动填的 API key 明文存在本机 SQLite 文件里，不加密——现在这个工具单机跑、单人用，这个风险可接受；等真的要多人共享或部署到服务器，这块必须重做（至少加密存储，理想是不落库、走系统 keychain 或环境变量）。首页填 key 的表单要显式提示这一点，不能让用户以为它是安全存储。

### 7.4 首页 + 侧边栏导航（Streamlit 多页应用）

用 Streamlit 原生多页机制（`st.navigation` + `st.Page`），不用手搭 session_state 路由：

```
app_streamlit/
├── Home.py                 -- 入口：项目列表、新建项目、API/模型设置
├── pages/
│   ├── project.py          -- 项目工作区：定量问卷调研/定性访谈/案头研究 三个分区
│   └── analysis.py         -- 单份分析详情页：左侧 sidebar 放①～⑩锚点，主区渲染现有①～⑩逻辑
└── ...engine 不变...
```

`analysis.py` 的 sidebar：`st.sidebar` 里放①～⑩标题的按钮/链接，点击后用 Streamlit 的锚点跳转（每个模块标题渲染时同时生成一个 HTML anchor id，sidebar 链接指向 `#anchor-id`）；"隐藏 sidebar"用 `st.session_state` 存一个开关，配合 `st.set_page_config(initial_sidebar_state=...)` 或直接用 Streamlit 自带的 sidebar 收起箭头（Streamlit 原生就有，不用自己写）。

### 7.5 Word 导出（第 7 点）

新增 `engine/export_word.py`，规则**直接照抄** `~/Desktop/Skills/问卷整理_Word汇报版-1.3.md`——这份 skill 本来就是给人（或 AI）手动写 Word 报告用的规格书，现在把它翻译成 `python-docx` 代码：标题格式、图表转图片、数字格式「XX人（XX.X%）」、开放题摘录表、命名规则全部照 skill 走，**不再另定一套规则**。v1 范围：①②③④⑤单选/多选/数值题的图+核心发现、开放题摘录表；⑧交叉表；⑨AI洞察。⑥⑦（原始数据表格/点击下钻）不导出——那是网页交互功能，Word 是静态文档，本来就没有这两块的对应物。

### 7.6 里程碑

7. **Milestone 7**：多供应商 LLM（`OpenAICompatibleProvider` + 注册表 + 首页设置表单）
8. **Milestone 8**：Schema 扩展二轮（`research_type`/`title`/`updated_at`/`meta_json`）+ `save_analysis`/`load_analysis`
9. **Milestone 9**：Streamlit 多页应用骨架（首页 + 项目工作区 + 分析详情页路由）
10. **Milestone 10**：analysis.py 接入持久化（生成分析即保存、打开历史分析即读回）+ sidebar 模块导航
11. **Milestone 11**：Word 导出

---

## 版本历史

- v0.1～v0.3：见对应文件。
- **v0.4**（2026-09-13）：落地 10 部分整体页面框架。新增术语"分流/跳转"并反向同步进 Word 1.3、Excel 1.2 两份 SOP skill 的"确认数据"步骤。Schema 新增 `test_method`、`conclusions` 表，`documents` 加 `branch_label`，`questions.is_screen_out`（布尔）改为 `section`（四态枚举：筛选/正式/基础信息/平台自动）。对 ⑥ 原始数据表格给出可行性评估，建议引入 `streamlit-aggrid` 一次性解决分组表头着色和点击下钻两个需求。留一个待确认问题：筛选题/基础信息题是否要重新编号。
- **v0.5**（2026-09-13）：确认题号方案——筛选题 `S1/S2…`、基础信息题 `C1/C2…`、正式问卷保留原题号 `Q1…`，三段各自独立编号。Schema 迁移（`section` 字段 + `test_method`/`conclusions` 表）已由 Codex 实现并独立验证通过。
- **v0.6**（2026-09-13）：①②③④⑤ 在 `app_streamlit/app.py` 里实现（session_state 版，未接 SQLite）。修复"点击生成分析后交互控件会清空结果区"的真实 bug。更正 v0.5 里"之后会同步改 Word skill"的过度承诺——S/C/Q 编号是网页工具专属结构，Word/Excel 两份 skill 保持不动。
- **v0.7**（2026-09-13）：10 部分整体框架①～⑩全部在 demo 里接了一版（session_state，未接 SQLite）。新增 `engine/ai_insight.py`（AI 洞察，数字精确 token 校验）。引入 `streamlit-aggrid` 做⑥⑦。
- **v0.8**（2026-09-14）：新阶段——首页+项目列表+持久化+多供应商 LLM+侧边栏模块导航+Word 导出（第 7 节）。Schema 再扩一轮：`documents` 加 `research_type`/`title`/`updated_at`，`questions` 加 `meta_json`，`settings` 新增按供应商存 key 的约定。多供应商用一个 `OpenAICompatibleProvider` 复用 5 家（OpenAI/DeepSeek/Kimi/Qwen/Grok），Claude 保留独立实现。UI 从单页 demo 改成 Streamlit 多页应用（首页/项目工作区/分析详情页）。
- **v0.7**（2026-09-13）：10 部分整体框架①～⑩全部在 demo 里接了一版（session_state，未接 SQLite）。新增 `engine/ai_insight.py`（AI 洞察，数字精确 token 校验，不是子串匹配）。新增依赖 `streamlit-aggrid` 做⑥⑦；数据组装逻辑单独验证过，表头着色和点击选中的真实效果还没人工验证过，明确记在第 4 节末尾。⑧交叉分析对比对象仅支持单选题。导出 Word 还没做。
