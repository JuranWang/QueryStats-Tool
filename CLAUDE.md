# 给未来在这个仓库里干活的 Claude Code / Codex 会话

这份文件不是功能文档（功能文档见 `README.md`/`README.zh-CN.md`、`设计文档-v0.*.md`），
是这个项目开发到 2026-09-22 为止，反复踩坑、反复被真实反馈纠正之后总结出来的**协作心得**。
目的是让下一次 coding 效率更高，而不是同样的问题反复出现、重复解决。

## 一、核心注意点（技术层面）

1. **Streamlit 的 rerun / session_state 模型**
   - widget 一旦带 `key`，往后 `value=`/`default=` 参数只在这个 key **第一次**被创建时
     生效，之后每次 rerun 都会被忽略——想强制刷新界面上显示的值，要么清空/换掉这个
     key，要么走 `st.session_state[key] = 新值` 直接赋值（不能靠重新传 `value=`）。
   - `engine/*` 下面的模块级常量/缓存，改完代码必须重启 `streamlit run` 进程才会生效
     ——本地开发发现"改了看不出差异"，先怀疑服务器有没有重启，不要急着怀疑代码逻辑。
   - 项目里"切换到另一份历史文档"用的是"白名单 keep_keys + 其余全部清空重来"的策略
     （`app_streamlit/app.py` 里 `analysis_mode == "load_existing"` 那段），不是逐个列出
     "记得清哪些 key"——新加 session_state key 不用手动维护清理清单，但要理解这个
     机制的存在，别把不该跨文档保留的东西塞进 `keep_keys`。

2. **SQLite 迁移**
   - `CHECK` 约束改不了，只能整表重建（rename → create 新表 → copy 数据 → drop 旧表）。
   - `ALTER TABLE ... RENAME` 默认会把**其它表**指向被改名的表的外键定义也重写掉，
     必须在 rename 那一步套 `PRAGMA legacy_alter_table = ON`。
   - `PRAGMA foreign_keys` / `PRAGMA legacy_alter_table` 在一个活跃事务（Python
     `with conn:` 块）里面设置会被静默忽略，必须在事务块外单独执行。
   - 任何迁移函数写完，先写一个独立的最小复现脚本验证，再套到真实迁移逻辑里，
     不要直接在正式迁移函数里试错。

3. **i18n**
   - `tests/test_i18n.py` 只扫描**含中文字符**的 `t()` 调用；纯符号/占位符字符串
     （比如 `t("｜{option}")`）它抓不到，测试绿了不代表翻译真的补全了——新增文案后
     人工过一遍 `engine/i18n_catalog_app.py`，确认真的加了对应英文条目。

4. **Codex 沙箱的已知假阴性**
   - `test_export_pdf.py::test_real_pdf_conversion` 这类调 LibreOffice 的测试，在
     Codex `exec` 自己的沙箱环境里会必现 `Abort trap: 6`；同一份代码脱离沙箱重跑
     就正常通过。这是环境限制，不是代码缺陷，遇到不用花时间"修"，独立重跑一遍确认
     不是别的问题就行。

5. **原生组件优先于自定义 HTML/JS**
   - `st.markdown(unsafe_allow_html=True)` 会把 `onclick` 这类事件属性静默过滤掉
     （真机验证过：渲染出来的标签上属性整个不见了），JS 事件绑定这条路线在这个
     沙箱化的渲染环境里基本走不通。
   - 浏览器不允许从链接点击直接跳转到 `data:` URI（会打开空白页）。
   - `streamlit-elements`（react-grid-layout）这类第三方组件，在这个项目里两次
     "自由拖拽/调整大小"的场景都没能在真实浏览器里跑出预期效果，且维护活跃度存疑。
   - 结论：遇到"排版/居中/拖拽"这类交互需求，先看 Streamlit 原生控件（列布局、
     `st.popover`、上下移动按钮）能不能凑出足够好的体验，不要急着上第三方 JS 组件；
     真的要拼 HTML/JS，优先用 `st.components.v1.html`（完全独立 iframe，不依赖
     `unsafe_allow_html` 的事件属性过滤问题）而不是 `st.markdown(unsafe_allow_html=True)`。

## 二、怎么设计交互体验会更好

1. **"自动识别 + 人工可改"优于"纯自动"或"纯手动"**——排序题类型检测、开放题
   关联候选题目、图片排版对齐方式，都是"给一个大概率对的默认值，同时留一个明显
   的入口去改"，比强制全自动（用户没法纠正误判）或者让用户从零手动配置（大部分
   情况其实有规律可循）都体验更好。

2. **别急着加"可调节滑块"，先想清楚默认值该是什么。** 图片排版这个功能来回改了
   三版：第一版是自由拖拽调整大小/位置，第二版是每张图各自一个缩放滑块，两版
   用户反馈都是"不好用"；最后真正解决问题的是一个**固定规则**（基准高度=饼图
   高度的 2/3，一行放不下预设张数才整体等比缩小），不是更多可调选项。遇到
   "这个东西的大小/位置要不要能调"这类需求，先反问一句"有没有一个明确、可
   推导的默认规则能覆盖大多数情况"，把设计难题偷懒甩给一个滑块通常不是真正的解法。

3. **通用能力要靠约定复用，不要靠记忆重写。** 图片管理组件
   (`render_image_attachments_trigger`/`render_image_attachments_grid`) 只需要一个
   字符串 `q_no` 就能挂到任何"题目"甚至任何"板块"上（这次交叉分析重构就是直接
   传一个 `f"crosstab_block_{block_id}"` 复用了整套图片上传/落库逻辑，没有改动
   这两个函数一行代码）。新功能需要"图片/复制下载/自动保存"这类能力时，先想
   能不能通过遵守同一套 key 命名约定直接嫁接，而不是重新实现一遍。

4. **让真实浏览器验证成为强制步骤，不是可选步骤。** 这个项目里好几次"读代码
   看起来没问题"的实现，在真实浏览器里是错的：单张图片没有真正居中（原因是
   flex 容器缺 `width:100%`）、复制按钮颜色和横条对不上（用错了主题变量）、
   点图片跳转到空白页（`data:` URI + 浏览器安全策略）。这类问题的根源大多是
   CSS 层叠、iframe 沙箱边界、组件内部封装细节——这些东西读源码判断不出来，
   必须靠真实交互（Playwright 截图/点击）才能确认。

## 三、反复提及、容易出问题的地方

### 自动保存——这是这次被明确要求要深入想清楚的一点

现状（截至这次重构前）是**三条互不统一的路径**：

1. 手动「保存」按钮：全量覆盖，需要用户主动点。
2. 20 秒一次的 `autosaves` 草稿表：确实在写，但**正常重新打开一份分析走的是
   `load_analysis` 读正式数据，从来不读这张草稿表**——它只在"浏览器崩溃/意外
   关闭后能不能捞回来"这个场景才有意义，但这个场景至今没有对应的读取入口，
   实际效果等于摆设。
3. 一批"点了立刻落库"的即时保存函数（`_persist_images`、`_persist_ai_results`，
   这次新增的 `_persist_crosstab_blocks`）：都是被"结果下次打开就没了"这类
   真实反馈一个个补出来的，此前没有一条统一的判断规则来决定"什么时候需要这样
   的即时保存"。

**深入之后应该确立的原则**（供以后新功能直接套用，不用每次重新想一遍）：

- 区分"配置过程中的中间状态"和"一次有意义的产出"。像 AI 分类结果、插入的图片、
  交叉分析生成的结果表，共同特征是：**用户点了一个明确的动作按钮，得到一个
  值得跨会话保留的产出**。这类动作完成后应该立刻落库，不依赖、也不等待手动
  保存或定时草稿兜底。
- "正在调的挡位"（下拉框选到一半、维度名字还没填完、还没点生成按钮）**不需要**
  每次 rerun 都落库——这类状态就算丢失，代价也很小（重新选一下而已），过度
  即时保存反而增加数据库写入频率、拖慢交互，性价比不高。
- 20 秒自动草稿表这个机制目前处于"名义上存在、实际从不生效"的状态，这是**技术
  债**，不是可以忽略的细节——留着不用容易让人误以为"反正有自动保存兜底"而
  疏于给新功能加即时保存。以后要么给它接一个真正会被用到的"异常恢复"入口
  （比如页面顶部检测到有未读的草稿时提示"是否恢复"），要么明确废弃、从代码里
  删掉，不要放着不管。
- **判断新功能要不要即时保存的具体问句**：这个功能有没有"用户点一下、得到一个
  值得保留的结果"这个动作？有的话，那个动作对应的处理函数末尾就应该直接调用
  一个 `_persist_xxx()`，不要假设"应该会有别的机制兜底保存"。

### 其他反复出现的问题

- **session_state key 串号**："重新打开另一份文档，看到的却是上一份的旧数据"
  这一类 bug，根源几乎都是某个 widget key 在切换文档时没有被正确清空。现在的
  方案是"`keep_keys` 白名单 + 其余全部清空"，新增 key 不需要手动维护清理清单，
  但要意识到这个机制的存在——如果某个新 key 恰好需要跨文档保留（目前只有
  `db_conn`/`lang`/`analysis_mode`/`current_document_id`/`current_project_id`
  这几个基础设施类的），要显式想清楚为什么，不要顺手往 `keep_keys` 里加东西。
- **默认值下旧行为必须完全不受影响。** 每次给已有功能加"可配置项"（这次交叉
  分析重构里，给"对比到哪道题"那一侧也加上了跟"圈人群"那一侧一样的维度分组
  能力），都要专门验证：**在用户完全不碰新配置项、维持默认值的情况下，跟改动
  前的行为逐字节一致**——这次专门写了这样一条回归测试（旧代码 vs 新代码，同
  一份合成数据，断言结果表相等），这个习惯要在以后的每次重构里延续。
- **看起来对、实际没对齐/没居中的视觉细节**，往往是缺一行 `width: 100%` 之类的
  CSS 基础属性，光靠读代码判断不出来，必须靠真实截图。

## 四、新增功能前的自查清单（对应"一定要 double check"的要求）

写代码前先过一遍：

1. 这个功能有没有可以直接复用的现成模式？（图片管理组件、复制/下载按钮、
   `_persist_xxx` 即时保存模式、`t()` 国际化写法）——优先复用，不要重新发明。
2. 这个功能会不会新增 session_state key？切换文档时它应不应该被清空？
3. 这个功能有没有"用户点一下、得到一个值得保留的产出"的动作？有的话，产出
   后要立刻调用对应的即时保存函数，不能只指望手动保存/定时草稿。
4. 新增的每一处用户可见中文文案，有没有全部包 `t(...)`；有没有同步补全
   `engine/i18n_catalog_app.py` 里的英文翻译（`test_i18n.py` 测试绿了不代表
   补全了，纯符号/占位符字符串它扫不到）。
5. 涉及排版/居中/对齐这类视觉细节，有没有计划用真实浏览器（Playwright）截图
   验证，而不是只靠读代码判断"应该没问题"。
6. 涉及数据库结构变化，有没有考虑到 SQLite `CHECK` 约束/外键在整表重建时的坑，
   有没有先写独立复现脚本验证迁移逻辑。
7. 改完一个已有功能，有没有专门验证"默认值/不使用新选项时，行为跟改动前完全
   一致"（写一条明确的回归测试，不是口头觉得"应该没问题"）。
8. 完成后，**独立**重新跑一遍测试和验证（不管是自己写的代码还是 Codex 代出来的
   代码，都不能只看"自称通过"的总结）——语法检查、真实 import、真实浏览器
   点击一遍关键交互；PDF/LibreOffice 相关测试在 Codex 沙箱里失败是已知假阴性，
   其它任何失败都要认真排查到根因，不能归因于"环境问题"就跳过。
