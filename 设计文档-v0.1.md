# 问卷可视化分析工具 · 设计文档 v0.1

> 版本记录规则：每次设计有实质性变化，另存一份新版本文件（v0.2、v0.3…），不覆盖旧版本。
> 本文件是 v0.1，形成于 2026-09-13，与用户的讨论过程见文末「版本历史」。

---

## 1. 项目背景

内部已有一套问卷分析 SOP，目前以两份 skill 文件的形式存在（`问卷整理_Word汇报版` / `问卷整理_Excel原始数据版`，均在 `~/Desktop/Skills/`），靠人在 Claude Code 对话里手动执行。问题：
- 每次都要重新走一遍确认数据、清洗、画图的流程，重复劳动；
- 图表规则、数字格式、清洗底线这些"硬规矩"写在 prompt/skill 文字里，AI 遵守程度不稳定，容易偷懒漏做；
- 给客户看的是静态 Word/Excel，交互性和可视化表现力有限。

目标：做一个网页工具，把 SOP 里能写成确定性规则的部分**变成 Python 代码**（不再依赖 AI 每次"记得"照做），只在 Python 真正做不了的地方（开放题语义分类、双语翻译）按需调用 AI API。用途：内部团队分析 + 未来可授权客户只读浏览。

---

## 2. 当前阶段范围（重要：与后期范围分开）

**做：**
- 本地单机运行、CSV 上传分析、图表可视化、原始数据可折叠查看、按需触发 AI 分类/翻译。

**暂不做（明确推迟，不是不做，是先不设计细节）：**
- 部署上线（先在本地测试打磨，跑通了再挑部署方案）；
- 内部团队登录/多账号体系（先单机单用户用）；
- 客户只读分享链接的鉴权落地（表结构先留好位置，逻辑先不写）。

这两块推迟不影响当前架构，理由见第 4 节「预留但不实现」的做法。

---

## 3. 已确认的关键决策（截至 v0.1）

| 决策点 | 结论 |
|---|---|
| 部署形态 | 先本地测试，部署方案后定；如果部署，目标是"对外可访问、客户只读链接"，不需要客户账号体系 |
| 内部团队登录 | 后期再加，当前单用户本地使用，不做鉴权 |
| AI 开放题分类 | 两种模式都要支持，每次触发 AI 分析时勾选："封闭分类"（用预定义类目）或"开放聚类"（AI 自己总结类目，可事后固定为封闭类目复用） |
| 文件格式（v1） | CSV 优先，解析层从一开始按 CSV + xlsx 两种写，UI 先只露 CSV 入口 |
| 语言 | 需要双语（英文问卷原文 + 中文报告），开放题原始数据要保留「原文｜中文翻译」双列结构，翻译可用 AI API 辅助但要走校验（见第 6 节） |
| Python vs AI 边界 | 能确定性计算的（计数、占比、排序、图表选型、清洗规则）一律 Python 硬编码；AI 只做语义判断类任务（开放题分类、逐句翻译），且 AI 的输出必须经过 Python 校验/复核，不直接采信 |

---

## 4. 架构

- **后端**：FastAPI（Python），本地用 `uvicorn` 跑。
- **前端**：React + Vite + ECharts（图表要支持点击下钻到原始回答，未来做"可视化突破"需要这个交互能力）。v0.1 阶段先做够用的版本，不追求视觉打磨。
- **数据库**：SQLite，单文件，本地即可。
- **AI 调用**：Anthropic API（Claude），通过后端统一封装的两个函数调用——`classify_open_ends()` 和 `translate_verbatims()`，前端不直接碰 API key。

**预留但不实现（给部署/登录留口子，现在不花时间做逻辑）：**
- `share_links` 表结构定义好，字段留着，暂时没有任何路由使用它。
- 后端用户身份走一个 `get_current_user()` 依赖注入函数，现在固定返回本地用户，以后换成真登录只改这一个函数。

---

## 5. 模块拆分

1. **上传 & 解析**：CSV/xlsx 读入，编码探测（GBK/UTF-8-BOM），生成列预览。
2. **数据确认（关键手动步骤）**：上传后进入映射页，给每一列打标签——题号、题型（单选/多选/开放/数值/ID）、英文原题、是否为 screen out 判据。这一步对应现有 skill 的"第1步：确认数据"，网页版没法聊天问，只能做成表单。
3. **清洗层**：去重、screen out 剔除、A/B 合并，做成可勾选的规则列表，非硬编码脚本。
4. **统计引擎**：按题型算分布/排名/均值中位数/交叉表，规则照搬两份 skill 文件里的"排序""数值题""交叉对比"条款。
5. **图表层**：统计结果 → ECharts 配置。单选 ≤5 饼图、>5 横向柱、多选横向柱；配色直接复用 skill 参数表的色号（`1F3864` 等），网页和 Word 报告视觉统一。
6. **原始数据区**：默认折叠，只有开放题（或手动勾选"需要看原始"的题）展开；开放题行显示「对应选择｜原文｜中文翻译｜（AI 分类后）标签」。
7. **AI 分类模块**：按需触发，非自动跑。
   - 封闭模式：用预定义类目表 `categories(question_id, label)`，AI 返回 `{response_id, category_id}`；
   - 开放模式：AI 自己提议类目，落一张暂存表，人工审核/改名后可"固定"转成封闭类目表，供以后复用；
   - **计数和百分比永远由 Python 算，AI 只出标签，不出数字**。
8. **AI 翻译模块**：按需触发，翻译开放题原文。规则同现有 skill 底线——逐句翻译、不概括、数字与品牌名不译。Python 侧做校验：原文里的阿拉伯数字必须原样出现在译文里；配置一份品牌名词表，出现在原文里的词必须原样出现在译文里；校验不过的行标红，退回人工修改，不允许 AI 说"翻完了"就直接采信。
9. **导出**：导出 Word 汇报版（复用两份 skill 的排版规则）、导出统计报告。

---

## 6. 数据模型（SQLite，草案）

```
projects(id, name, source_lang, target_lang, created_at)
documents(id, project_id, filename, format, uploaded_at)
questions(id, document_id, q_no, q_type, source_text_en, order_index, is_screen_out)
responses(id, question_id, respondent_id, raw_value, translation, order_index)
categories(id, question_id, label, source)         -- source: manual / ai_closed / ai_open_frozen
response_categories(response_id, category_id)      -- 多对多，一条开放题回答可能属于多个类目
ai_runs(id, question_id, mode, prompt_version, raw_output, created_at)   -- 审计用，记录每次 AI 调用
share_links(id, project_id, token, scope, expires_at)   -- 预留，当前无路由使用
settings(key, value)
```

`ai_runs` 是新增的一张审计表：每次调用 AI（分类或翻译）都留一条记录（用的哪个 prompt 版本、原始返回），方便以后排查"这次分类结果为什么长这样"，也方便复算。

---

## 7. 风险与坑

- **列映射靠人工，无法完全自动化**——CSV 本身不会自描述题型，映射表单这一步不能省，也不该硬省，省了后面全错。
- **CSV 编码/引号问题**——中文导出常见 GBK 或 UTF-8-BOM 混用，字段里带逗号要正确处理，需要用 pandas 的健壮读取而不是手写 split。
- **开放聚类的类目不稳定**——每次重新聚类类目名可能不一样，这也是为什么设计了"固定为封闭类目"的转换路径，否则没法跨批次比较。
- **AI 翻译/分类的成本和延迟**——按需触发（用户点按钮）而不是自动全跑，避免不必要的 API 调用。
- **双语校验的品牌名词表需要维护**——首次搭建时要把已知品牌名（Ruggable/IKEA 等）录入配置，后续遇到新品牌要记得加，否则校验会误判或漏判。

---

## 8. 待确认问题（开始写代码前）

1. AI 供应商/模型：直接用 Claude API（跟这个对话环境一致），还是要支持切换供应商？
2. 开放题预定义类目：由你在映射页手动输入，还是希望工具给一版"常见类目"起点模板（品质/价格/设计/其他之类）供你改？
3. 品牌名词表、专业词表（cut pile / loop pile 这些）是复用现有两份 skill 参数表里的清单，还是要另建一份更完整的？
4. 交叉对比、数值题这些复杂统计，v1 先只做「和现有 skill 覆盖范围一致」的部分，还是要加显著性检验（卡方检验等，附录提到的 scipy/statsmodels）？

---

## 9. 里程碑（本地 MVP，不含部署阶段）

1. 第 1 周：项目骨架、SQLite、CSV 解析、数据映射表单
2. 第 2 周：清洗层、统计引擎、图表层（复用 skill 图表规则）
3. 第 3 周：原始数据折叠区、AI 分类模块（封闭+开放两种模式）
4. 第 4 周：AI 翻译模块 + 校验、导出 Word、内部试用

---

## 版本历史

- **v0.1**（2026-09-13）：首次成文。来源对话：先讨论「本地化翻译辅助工具」需求交接文档，用户随后转向讨论本项目（问卷可视化分析工具），确认了 AI 分类模式、部署与登录延后、CSV/xlsx 范围、双语需求等关键决策。对应 Claude Code 会话见同目录 `resume-claude-code.md`。
