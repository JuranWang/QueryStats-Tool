# 问卷可视化分析工具 · 设计文档 v0.10

> 版本记录规则：每次设计有实质性变化，另存一份新版本文件，不覆盖旧版本。v0.1～v0.9 见同目录。
> 本文件 v0.10，形成于 2026-09-15。这一版没有新的里程碑编号——是用真实用户上传的多份问卷文件
> （Tally+Prolific、见数/Credamo）反复测试暴露出来的一轮真实 bug 修复 + 若干用户直接提出的功能
> 补强，规模不小，集中记一版。v0.9 记的 4 个已知缺口里，2 个在这一版解决，1 个部分解决（见第 4 节）。

---

## 1. 这一版做了什么（按功能分组，不按时间顺序）

### 1.1 数据摄入健壮性——全部是拿真实文件复现出来、不是假设的 bug

- **多选题自动分组识别**（`engine/clean.py` 新增 `detect_multi_select_groups`）：见数/Credamo
  用"题干-选项"短横线拆分，Tally 用"题干 (选项)"括号拆分，之前完全没人认，每个选项列被当成
  独立单选 True/False 题。现在按列名规律 + 取值是否以布尔值为主两道判断自动分组，合并成一道
  multi 类型的题，汇总列（Tally 会多导出一列逗号拼接的"选中了哪些"）默认忽略。
- **嵌套括号解析 bug**（真实数据复现）：选项文字本身带括号说明时（比如
  `"Warm air blown through vents/ducts (forced air)"`），原来用 `rindex(" (")` 找最后一个左括号，
  嵌套的时候会找错，切出来的"题干"/"选项名"都是半截。改成按括号深度从后往前找配对括号，
  `_multi_group_prefix` 和 `option_labels_for_group` 两处同样的 bug 一起修。
- **`looks_like_metadata_row` 误删真实数据的 bug**（真实数据复现，比上面这个更严重）：一份多选题
  很多的问卷，一个真实受访者的答案里 True/False 占了大半（每道多选题拆出来的每个选项各占一列），
  "True"/"False"字面上正好符合"像内部字段代码"的粗筛条件，比例冲过 70% 判定线，整行被当成
  见数/Credamo 那种表头行误删——这不是识别多选题识别错了，是**真实受访者的一整行答案被静默丢弃**。
  已修：真实布尔值不计入"像代码"的判断。
- **`_is_truthy` 不认字符串 "1"/"0"**（真实数据复现）：见数导出的多选布尔列有的是字符串
  `"1"`/`"0"` 不是原生数字/布尔值，之前字符串分支只认"是"/"yes"，导致一整道多选题 150 人
  全部判定成"没选任何选项"，图表是空的。
- **Credamo 平台字段误入正式问卷**：作答ID/用户ID/开始时间/经纬度/屏幕分辨率等 16 个 Credamo
  固定收集的字段，之前会被当成正式问题。改用精确匹配（不是子串匹配——"省份""城市"这种词如果
  子串匹配会连"您所在的城市是？"这种真实题目一起误伤）的已知字段名单，命中就归到⑩受访者信息。
- **元数据行识别范围扩大**：见数两行表头，字段代码有时不是干净的"Q5_1"，是"Q1_1_原题干"这种
  代码前缀拼接原题干的形式（分流/多轮重复题常见），原来的纯代码正则认不出来，现在额外认
  "字母+数字+下划线"开头的前缀。

### 1.2 保存机制——从"生成那一刻的快照"变成"手动/自动两条线，互不覆盖"

对应 v0.9 缺口 3、4。

- **`autosaves` 表**（单槽覆盖，`document_id` 主键）：不用点任何东西，映射/筛选/测试方法/结论/
  AI分类结果/图片/排版设置只要跟上次保存不一样、且离上次自动保存超过 20 秒，就整份写进这张草稿表。
- **手动保存按钮**：调用新增的 `persistence.update_analysis`（原地整份覆盖已有 document，不新建、
  结论整份替换不累加），成功后清掉对应的自动保存草稿。
- **`document_extras` 表**（同样单槽覆盖）：AI 分类结果、插入的图片（含说明文字）、"每行放几张"、
  图表文字覆盖这几项形状不适合拆关系表的内容，整份存 JSON blob，跟着手动/自动保存一起写。
- **项目自动命名**：分析页面没有项目上下文时（比如服务端重启后浏览器还停在分析页）会自动开一个
  项目，不再叫"未命名项目"——配了 AI 供应商就用 AI 从题目里总结一个有区分度的名字，没配就用
  Python 挑一道代表性问题当名字；这类项目打 `origin='auto'` 标记，首页可以按"自动生成/手动新建"
  筛选，不会跟真正建的项目混在一起（老数据库回填过一次：名字精确等于"未命名项目"的历史项目
  自动标成 `origin='auto'`）。
- **项目/问卷的重命名、删除**（对应 v0.9 缺口 4）：级联清理 questions/responses/autosaves/
  document_extras，不留孤儿行。

### 1.3 图片/图表排版——两次尝试拖拽方案都不理想，换成更简单可靠的做法

- **第一次尝试**：`streamlit-elements`（react-grid-layout）自由拖拽 + 调整大小的画布。上线后
  发现拖到已占用的行不会自动等分宽度（这是 react-grid-layout 本身的固有行为，不是配置问题）。
- **第二次尝试**：写了一个纯 Python 的行内自动等分算法（`onDragStop`/`onResizeStop` 事件触发后
  重新计算），逻辑本身单元测试全过，但用户反馈实际效果跟预期不符，且没看到删除入口。
- **最终方案**：整个放弃 `streamlit-elements` 依赖，改成 Streamlit 原生 `st.columns`——每道题
  一个"每行放几张"数字输入框（图表可以勾选要不要参与排版），图片顺序用上移/下移按钮调整（不是
  拖拽），删除按钮直接可见。全部是标准控件，没有自定义 JS 组件，没有"这个事件到底有没有真的被
  触发"这类没法在无浏览器环境验证的不确定性。之前给自由拖拽用的 `engine/dragdrop_layout.py`
  连同它的测试一起删除（不是这次真正用上的东西）。
- **file_uploader 的一个经典坑**：只要 key 不变，它会一直记得这次选过的文件、每次 rerun 都原样
  交还，不是"只在选文件那一次"才返回——导致删除图片之后紧接的那次 rerun 又把刚删的图片加回来。
  修法：每次成功吃进新文件后，换掉这个控件的 key 逼它复位。

### 1.4 图表文字与单张图表导出

- **图表文字手动编辑**：单选/多选题图表标题旁新增"编辑文字"弹窗，可以覆盖图例/坐标轴上任意
  一段自动生成的展示文字，只改显示，不影响背后统计数字。
- **单张图表下载/复制**：每张图表可以单独存一张 PNG（重新用 matplotlib 画一份，不是网页截图），
  顶部带这道题的中文标题（原题是中文用原文，是英文查翻译缓存），标题过长自动按字符数手动折行
  （试过 `plt.suptitle(wrap=True)`，配合 `bbox_inches="tight"` 会把画布越撑越宽而不是真的换行，
  不可靠，改成 `textwrap.wrap` 手动控制）。下载用 Streamlit 原生 `download_button`；复制到剪贴板
  用浏览器原生 Clipboard API，写一小段自包含的 HTML+JS 通过 `st.components.v1.html` 注入（这是
  Streamlit 自带的稳定核心功能，不是新的第三方组件，风险跟 1.3 节的拖拽库不是一个量级）——图标
  是内联 SVG（这段 HTML 在独立沙盒 iframe 里跑，拿不到主页面的图标字体）。这个功能没法在无浏览器
  环境里验证 Clipboard 写图片操作在具体浏览器上的实际效果。

### 1.5 开放题分析——支持按关联题目筛选后再跑 AI

用户原话"AI 分析这个步骤,尤其是开放题的回答,要能根据我选择的选项来分析"：

- 开放题可以关联同一段里前后 5 题以内的任意题目，默认关联逻辑是关键词判断（题干含"为什么/原因"
  这类追问词，默认关联紧邻的上一题），可以手动改。
- 关联后每个关联题目给一个筛选下拉（比如只看选了"空气能空调"的人），筛选结果同时驱动"原始数据"
  表格显示范围**和** AI 分类/聚类的输入范围——不再是"表格筛了但 AI 还是分析全部人"。
- 不同筛选条件的 AI 结果分开存（key 里带筛选条件），切换筛选条件之前跑过的结果还能直接看到，
  不用重新调用 AI。

### 1.6 翻译供应商与项目命名

- 翻译（题目/选项/开放题原文）跟"AI 分类/AI 洞察"这类通用用途可以配成两个不同的供应商——翻译
  量通常大得多，没必要全用旗舰模型的价格。新增 OpenRouter 支持（`base_url` 换一下即可，复用
  现有的 `OpenAICompatibleProvider`），并给了一份常用中英翻译模型的预设下拉（Qwen-Turbo/
  DeepSeek/Qwen-Plus/GPT-4o-mini/Claude Haiku 4.5，按价格从低到高），不用手输容易出错的模型名。
- 首页"API/模型设置"原来用 `st.form` 包着，切供应商/模型不会立刻刷新界面（这也是 OpenRouter
  预设下拉切"自定义"选项不生效的直接原因）。改成普通控件 + 手动保存按钮，选完立刻生效。

### 1.7 可发现性

- 生成分析之后，"数据映射"表格其实一直是可以继续改的（不用重新上传文件），只是不够显眼。这次
  在侧边栏模块导航最上面加了一条"调整题型／分组"快捷跳转，表格上方也加了一句话说明。

---

## 2. Schema 变更（在 v0.9 基础上再加两张表 + 一个字段）

```
autosaves(document_id PRIMARY KEY REFERENCES documents(id), payload_json, saved_at)
    -- 自动保存草稿，单槽覆盖。跟"正式"的 questions/responses 完全是两套数据，
    -- 结构上就不可能覆盖手动保存的内容。

document_extras(document_id PRIMARY KEY REFERENCES documents(id), payload_json, updated_at)
    -- AI 分类结果 / 图片（含字节，base64）/ 每题"每行放几张" / 图表文字覆盖，
    -- 形状差异太大不适合拆关系表，一份 JSON blob，跟 questions.meta_json 是同一个思路。
    -- 手动保存 / 自动保存都会写，前者整份覆盖（没有的内容也会清空旧值），后者是草稿。

projects(..., origin TEXT NOT NULL DEFAULT 'manual' CHECK (origin IN ('manual','auto')))
    -- 'auto'：分析页面没有项目上下文时系统代劳建的占位项目；'manual'：用户自己建的，
    -- 或者被人手动重命名过（重命名会把 origin 转回 'manual'）。
    -- 迁移策略：老数据库用 ALTER TABLE 补这一列，并把名字精确等于"未命名项目"的
    -- 历史项目一次性回填成 'auto'（这类项目在改动前的代码里名字是写死的，可以确定来源）。
```

`_migrate_add_missing_columns`（`engine/db.py`，`init_db` 里调用）是这次新加的迁移钩子：
`CREATE TABLE IF NOT EXISTS` 对已经存在的表不会补新列，SQLite 的 `ALTER TABLE ADD COLUMN`
也没有 `IF NOT EXISTS`，所以每次 `init_db` 先查 `PRAGMA table_info`，缺列再补，不重复执行。

---

## 3. 架构上的一个小结论：什么时候该用第三方 Streamlit 组件

1.3 节两次拖拽尝试失败后的复盘，值得记下来当以后的判断依据：

- `streamlit-elements` 这类包"能用"（`views/dragdrop_test.py` 隔离测试确认过能拖、能调大小）
  不等于"接进真实业务场景后行为符合预期"——它只是薄薄一层包装 react-grid-layout，任何自定义
  事件绑定（`onDragStop`/`onResizeStop`）能不能生效，Claude 在没有浏览器的环境下只能读源码验证
  "理论上应该转发"，没法验证"实际是不是真的转发了、时机对不对"。
- 需求本身如果能用 Streamlit 原生控件（`st.columns`/`st.button`/`st.download_button`/
  `st.components.v1.html` 这种一等公民 API）拼出来，即使体验上差一点（"每行放几张+上移下移"
  不如自由拖拽直观），也应该优先选原生方案——省掉的是一整类"猜测行为、等用户反馈、再猜测"的
  来回成本。这次两轮拖拽方案的调试成本，比直接换原生方案再加起来还高。
- `st.components.v1.html` 本身不算"第三方组件"，是 Streamlit 核心自带的稳定功能（渲染一段
  自包含的静态 HTML/JS 在沙盒 iframe 里），拿它做"复制到剪贴板"这种不需要 Python↔JS 双向通信
  的场景，风险跟接入一个独立维护的社区组件包完全不是一个量级。

---

## 4. 已知缺口（对照 v0.9 第 8 节更新）

1. **多选题选项级别的翻译持久化**——v0.9 记的缺口，这一版**修复工作已提交 Codex 执行**
   （`option_labels_for_group` 挪到 `engine/clean.py`，`persistence.py` 存储/查询翻译对照表都
   改用短选项名而不是原始布尔列的列名/取值）。结果需要独立验证后才能标记为真正解决——按这个
   项目的纪律，Codex 自报的"测试通过"不能直接采信。
2. **"打开历史分析"仍然没有真正接线**——v0.9 记的缺口，这一版**还没做**，是当前最大的一块
   遗留工作。跟缺口 1 强相关但不是同一件事：`load_analysis` 现在已经能把 `project_id` 也一起
   返回（这次顺手加的），但 `app_streamlit/app.py` 侧"如果是 load_existing 模式就跳过上传/映射
   步骤，直接用读回来的数据渲染"这段分支逻辑还没写。已知的一个结构性麻烦：加载出来的多选题
   `df_all` 每题只有一列（值是列表），跟实时上传流程里"多选题是好几个布尔兄弟列"的形状不一样，
   `render_unit` 的 multi 分支需要区分这两种情况。
3. **⑧交叉分析结果不接入 Word 导出**——v0.9 记的缺口，未变。
4. **Word 导出的单选/多选题目前没有走翻译展示逻辑**——这一版新发现，未修复：`app.py` 里"⑪ 导出
   Word"那段对单选/多选题是直接现算统计（`stats.single_choice_stats`/`multi_choice_stats`），
   没有像网页正文那样查 `translation_cache` 转成"中文／英文"，英文问卷导出的 Word 里图表可能
   是纯英文。开放题原文有专门现场调用 AI 翻译一遍，这个不受影响。
5. ~~项目/分析没有删除入口~~ ——**这一版已解决**（1.2 节）。
6. ~~自动保存是快照~~ ——**这一版已解决**（1.2 节，手动/自动两条线）。

### 下一步建议顺序

1. 独立验证 Codex 这次提交的多选题翻译持久化修复（不能只看它的自报总结）。
2. "打开历史分析"接线——工作量最大，涉及 `render_unit` 里 single/multi 两种数据形状的分支处理，
   建议先把"加载出来的 `units`/`df_all` 具体长什么样"这件事想清楚写成任务书，再决定哪部分交给
   Codex、哪部分自己做（这一版的经验是：牵涉"实时上传 vs. 读库回放"两条路径怎么合流这种需要
   反复判断的部分，自己做比来回 handoff 更快）。
3. Word 导出接入翻译（缺口 4）。
4. ⑧交叉分析结果接入 Word 导出（缺口 3）。

---

## 版本历史

- v0.1～v0.8：见对应文件。
- **v0.9**（2026-09-14）：M7～M11 全部完成并独立验证（76 测试）。`Home.py`/`views/home_view.py`/
  `views/project_view.py` 实际建好并接入 `app.py`。记录 4 个已知缺口（多选选项翻译持久化、
  历史分析读回未接线、非持续自动保存、无删除入口）。
- **v0.10**（2026-09-15）：用真实用户上传文件反复测试暴露出来的一轮 bug 修复（多选题分组识别、
  嵌套括号解析、元数据行误删真实数据、`_is_truthy` 布尔值判断、Credamo 平台字段误判）+ 若干
  用户直接提出的功能补强（自动/手动保存两条线 + `document_extras` 表、项目自动命名与筛选/
  重命名/删除、图片排版从两次拖拽尝试失败改为原生 `st.columns` 方案、图表文字手动编辑、单张
  图表下载/剪贴板复制、开放题按关联题目筛选后再跑 AI、翻译专用供应商与 OpenRouter 支持、
  数据映射表可发现性）。新增 `autosaves`/`document_extras` 两张表和 `projects.origin` 字段，
  新增数据库迁移钩子。v0.9 记的 4 个缺口，2 个解决、1 个提交 Codex 待独立验证、1 个仍未开始；
  新发现 1 个缺口（Word 导出单选/多选题未走翻译展示逻辑）。第 3 节记录了"拖拽方案两次不理想"
  的复盘结论，作为以后选型的判断依据。
